# Antigravity 개발 지침 — 업무 말투 변환기 (Biztalk Antigravity)

이 문서는 Antigravity 에이전트가 본 프로젝트를 개발, 수정, 테스트할 때 준수해야 하는 행동 규칙과 개발 원칙을 정의합니다. 상세 사양, API 명세, 완료 조건은 PRD 및 개요서를 참조하십시오.

---

## 1. 프로젝트 참고 문서
- **기능 및 API 명세**: [PRD_업무말투변환기.md](file:///C:/vibe_anigravity/biztalk_antigravity/PRD_업무말투변환기.md)
- **프로젝트 개요**: [개요서_업무말투변환기.md](file:///C:/vibe_anigravity/biztalk_antigravity/개요서_업무말투변환기.md)

---

## 2. Antigravity 핵심 행동 원칙 (Agent Rules)

### 🚨 바이브 코딩 3원칙의 에이전트 적용
에이전트는 모든 코드 작성 및 수정 작업 시 아래의 3원칙을 엄격하게 준수해야 합니다.

1. **원칙 1: 완료 기준을 먼저 정의하고 승인받기**
   - 새로운 기능 구현이나 리팩토링 시작 전, 작업의 "완료 기준"을 세부 체크리스트로 작성해 사용자에게 승인받아야 합니다. 임의로 범위를 확장하거나 불필요한 기능(예: DB 연동, 인증 등)을 추가하지 않습니다.
2. **원칙 2: 조사(Research) 먼저, 구현(Implementation) 나중**
   - `langchain-upstage`와 같은 외부 라이브러리 및 API를 사용하기 전에, 최신 사용법과 연동 제약사항을 우선 조사해 요약 보고한 뒤, 사용자의 확인을 받고 실제 코드를 구현합니다.
3. **원칙 3: 버그 발생 시 분석 먼저, 수정 나중**
   - 오류 발생 시 곧바로 코드를 수정(Replace)하지 않습니다. 에러 로그를 분석하여 발생 원인(Root Cause)을 정확히 파악하여 설명하고, 수정 방안을 합의한 후에 작업을 개시합니다.

### 🎨 프론트엔드 디자인 및 구현 원칙
- **Vanilla Tech Stack**: TailwindCSS 등의 CSS 프레임워크나 자바스크립트 라이브러리를 사용하지 않고 **순수 HTML5, CSS3, Vanilla JavaScript(ES6+)**만 사용합니다.
- **Visual Excellence (Aesthetic UI)**:
  - 브라우저 기본 스타일(Plain red, blue, green 등)을 사용하지 않습니다.
  - 가독성 높은 모던 웹 폰트(Google Fonts - Inter, Outfit 등)를 반드시 연동합니다.
  - 차분하고 세련된 HSL 테일러드 컬러 시스템을 적용하고, 로딩 바 및 토스트 메시지에 부드러운 전환 효과(Transition/Animation)를 적용하여 UX 완성도를 높입니다.

### 🐍 백엔드 구현 원칙
- **FastAPI 모범 사례**: Pydantic v2 기반 스키마를 사용하여 데이터 타입을 엄격하게 검증합니다.
- **안전한 CORS 설정**: 프론트엔드와 백엔드가 정상적으로 통신할 수 있도록 CORS 설정(`CORSMiddleware`)을 명확히 적용합니다.

### 🔒 보안 및 자격 증명 격리 (CRITICAL)
- **.env 파일 절대 노출 금지**: `.env` 파일은 민감한 API 키가 포함되어 있으므로 **절대로 터미널에 조회하여 출력하거나, 사용자에게 내용을 노출하거나, Git 커밋에 포함시키지 않습니다.**
- API 키는 메모리 상에서 `os.getenv` 또는 `python-dotenv`를 활용하여 주입받아야 합니다.

### 5. @PRD_업무말투변환기.md 문서와 AGENTS.md 문서 항상 최신화 하기
* 모든 변경사항이 발생하면 (예를 들어 기능이나 요구사항이 변경 되거나, 화면명세가 변경되거나, Source Code가 변경 되거나 라이브러리 버전이 변경되면) 관련된 markdown 문서들도 반드시 업데이트 합니다. 
* 구현이 완료된 사항들은 `@PRD_업무말투변환기.md\2. 완료 체크리스트`에 모두 체크표시를 해서 완료 되었음을 반드시 표시하세요.
* `@PRD_업무말투변환기.md\8. 단계별 구현 순서` 에서도 단계별로 구현이 완료되면 체크표시를 해서 완료 되었음을 반드시 표시하세요.

### 6. Plan Markdown 문서의 링크가 연결되지 않으므로 Plan을 요약해서 알려주세요.